# dolphinservice-soundkonverter
Adds a contextual menu on dolphin to launch SoundKonverter with audio files.

To install, just put the .desktop file of this repo in your `~/.local/share/kio/servicemenus`.
